import 'package:flutter/material.dart';

class SpotifyArtistCard extends StatelessWidget {
  const SpotifyArtistCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Hello Card'),
    );
  }
}
